import './index.css';

export default function Menu({ food }) {
    return (
        <section className="bg-pink-400 w-full h-full flex justify-center">
            <main className="bg-green w-[80%] menuGrid">

            </main>
        </section>
    );
}